#!/usr/bin/env bash

set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
LIST_FILE="${SCRIPT_DIR}/snap_apps.txt"

while read -r app; do
    snap install "$app"
done < "$LIST_FILE"

